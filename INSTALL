To compile it:
  mkdir build
  cd build/
  cmake .. -DCMAKE_INSTALL_PREFIX=/usr
  make -j3

To install it:
  cd build/
  sudo make install

Alternatively, you can also (if you are a developer), launch it using "./pantheon-terminal".
